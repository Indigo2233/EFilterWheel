# ESP8266 / Arduino Nano 电动滤镜轮实施计划

更新日期：2026-07-17  
参考资料：`E:\download\滤镜轮项目开源资料`

## 1. 项目目标

基于现有 3D 打印结构制作一套可重复定位的电动滤镜轮，支持以下两种主控构建配置：

- Arduino Nano：用于首个样机，通过 USB 串口连接计算机，优先完成机械、传感器和运动控制验证。
- ESP8266：用于网络控制版本，复用同一套运动控制逻辑，增加 Web 配置及 ASCOM Alpaca 接口。

两种主控为互斥配置，每台成品安装其中一种。首轮目标采用 Arduino Nano，原因是其 5 V I/O、USB 串口和原结构中的 ULN2003A/5 线步进电机组合适配度较高。

项目完成时应交付：

1. 可装配的机械结构及打印参数。
2. 原理图、接线图、BOM 和主控安装方案。
3. Arduino Nano 与 ESP8266 两个固件构建目标。
4. USB 串口控制协议及 ESP8266 网络接口。
5. ASCOM/Alpaca 兼容层和 N.I.N.A. 联调记录。
6. 校准、装配、烧录、故障处理和测试文档。

## 2. 已确认的参考基线

### 2.1 机械模型

| 模型 | 已提取外形尺寸 | 用途与检查重点 |
| --- | --- | --- |
| `7x36mm滤镜盘.STL` | 143 × 143 × 7 mm | 7 位、36 mm 滤镜；核对槽位中心与传感触发结构 |
| `5×2“滤镜盘.stl` | 143 × 143 × 7 mm | 5 位、2 英寸滤镜；核对滤镜实际外径和压环 |
| `外壳.STL` | 158 × 182.23 × 17 mm | 检查盘体间隙、遮光和主控空间 |
| `后盖.STL` | 158 × 182.59 × 5 mm | 检查螺丝长度、线束和主控高度 |
| `电机盖.STL` | 49.05 × 43.03 × 23 mm | 检查电机、接插件和线束空间 |
| `滤镜压环.STL` | 128.34 × 128.32 × 1 mm | 检查滤镜固定和翘曲 |
| `传动轮2_D轴.STL` | 11 × 11 × 8 mm | D 形电机轴适配候选 |
| `传动轮2_D14` 至 `D17` | 外径 14–17 mm，高 8 mm | 补偿打印收缩与摩擦圈预紧量 |
| `防脱压片.stl` | 33 × 33 × 3 mm | 中心轴防脱和轴向间隙控制 |
| `PCB螺丝垫片.STL` | 4.8 × 4.8 × 1 mm | 防止 PCB 与打印件接触磨损 |

参考 3MF 的现有切片参数为：PLA、0.4 mm 喷嘴、0.2 mm 层高、2 道墙、15% 网格填充、5 层顶面、3 层底面、关闭支撑、5 mm 自动裙边。首个尺寸样件沿用这组参数，传动轮应分别打印多个直径版本进行实配。

### 2.2 原结构的机电特征

根据项目介绍、内部结构图和焊接参考图，当前基线包含：

- 5 线单极步进电机和 ULN2003A 驱动阵列，具体电机型号及线圈参数需通过 BOM 和实物测量确认。
- 摩擦轮传动，传动轮通过橡胶圈压紧滤镜盘外缘。
- 一个霍尔传感器和一个磁铁，磁铁安装要求为 S 极朝外。
- 一个微动开关，预期用于槽位触发或独立位置校验，其实际触发规律应在手动旋转滤镜盘时记录。
- USB 接口、蜂鸣器、电源保护器件和 5 线电机插座。
- 上电初始化、位置检测和 ASCOM 控制属于原项目已说明的功能。

### 2.3 使用许可

参考资料采用仅限非商业用途的自定义许可。个人制作、学习和非营利协作可按许可进行；再发布模型、固件或修改版本时必须保留原许可与作者版权信息。商业用途需要取得作者书面授权。

## 3. 总体技术方案

```text
5 V 输入
  ├─ 保险/反接保护/TVS/滤波
  ├─ 主控：Arduino Nano 或 ESP8266
  └─ ULN2003A → 5 线步进电机 → 摩擦轮 → 滤镜盘

霍尔传感器 ──→ 原点输入
微动开关   ──→ 槽位索引或独立校验输入
USB 串口   ──→ Nano 控制协议 ──→ ASCOM 兼容层
Wi-Fi      ──→ ESP8266 HTTP/Alpaca 接口
```

固件分为四层：

1. `hal`：GPIO、定时器、EEPROM、串口和网络适配。
2. `motion`：步进序列、加减速、方向、超时和断电释放。
3. `wheel`：回零、槽位换算、误差补偿、状态机和故障处理。
4. `transport`：串口命令、Web 配置和 Alpaca API。

运动状态统一定义为 `BOOT`、`HOMING`、`READY`、`MOVING`、`ERROR`。所有移动操作均应设置最大步数和超时时间，故障状态应立即停止脉冲并释放电机线圈。

## 4. 电气设计计划

### 4.1 首轮器件基线

- 主控：Arduino Nano ATmega328P 兼容板。
- 网络主控候选：Wemos D1 mini 或同尺寸 ESP8266 开发板，最终型号由安装空间决定。
- 电机：参考原结构的 5 V、5 线减速步进电机候选，优先核对是否为 28BYJ-48 及其减速比。
- 驱动：ULN2003A 模块或自制小板；ESP8266 配置需实测 3.3 V 输入驱动余量。
- 原点传感器：开集电极霍尔开关候选，具体型号以原 BOM/丝印为准。
- 位置传感器：常开或常闭微动开关，先测定原机械触发方式。
- 电源：稳压 5 V、持续电流至少 1 A，建议预留 2 A 电源适配器余量。
- 保护与滤波：自恢复保险丝、反接保护、TVS、主控附近 100 nF 去耦和电机驱动附近 100–470 µF 储能电容。
- 连接：电机采用带锁 5Pin 插座，传感器采用带锁 2Pin/3Pin 插座，避免装配时反插。

### 4.2 候选引脚分配

Arduino Nano：

| 功能 | 引脚 | 说明 |
| --- | --- | --- |
| 霍尔原点 | D2 | 外部中断、内部或外部上拉 |
| 微动开关 | D3 | 外部中断、软硬件消抖 |
| 电机 IN1–IN4 | D4–D7 | 连接 ULN2003A |
| 蜂鸣器 | D8 | 通过限流或三极管驱动 |
| 状态灯 | D13 | 使用板载 LED |
| USB 串口 | D0/D1 | 仅用于板载 USB 串口，不接其他负载 |

ESP8266 候选：

| 功能 | GPIO | 说明 |
| --- | --- | --- |
| 电机 IN1–IN4 | 5、4、14、12 | 对应常见 D1、D2、D5、D6 标记 |
| 霍尔原点 | 13 | 中断输入，使用 3.3 V 上拉 |
| 微动开关 | 16 | 轮询输入并进行消抖 |

ESP8266 的传感器输入电压必须限制在 3.3 V。若霍尔器件为开集电极输出，将上拉电阻接至 3.3 V；若输出结构或型号无法确认，应增加分压、钳位或电平转换。GPIO0、GPIO2 和 GPIO15 与启动模式有关，首版接线应避免传感器改变其上电电平。

### 4.3 原理样机检查

1. 在滤镜轮外部搭建电机、ULN2003A 和主控。
2. 测量单相、双相和堵转时的 5 V 电流及主控电压跌落。
3. 确认电机线序、转向、半步序列、最大可靠速度和启动速度。
4. 分别测量霍尔传感器有磁/无磁电平，确认磁铁极性和有效距离。
5. 记录微动开关在完整一圈内的触发次数、触发宽度和对应槽位。
6. 验证电机停机后释放线圈不会导致滤镜盘自行偏移。

## 5. 机械实施计划

### 阶段 M1：模型复核

1. 确定首个滤镜盘版本，默认采用 `7x36mm滤镜盘.STL`。
2. 在切片软件中检查所有模型的单位、法向、薄壁、孔径和打印方向。
3. 测量现有电机轴、橡胶圈、磁铁、微动开关、螺丝和滤镜的实际尺寸。
4. 先打印 D14–D17 传动轮、PCB 垫片和一段局部配合测试件。
5. 选定能够稳定传动且不会显著压缩橡胶圈的传动轮尺寸。

### 阶段 M2：空载装配

1. 打印外壳、后盖、电机盖、滤镜盘、防脱压片和压环。
2. 安装空滤镜盘，调整中心轴向间隙，使盘体能够手动平稳旋转。
3. 检查盘体全周与外壳、后盖、螺丝、传感器和线束的间隙。
4. 调整电机位置和摩擦轮预紧量，记录可重复的装配尺寸。
5. 确定 Nano 与 ESP8266 各自的安装支架、USB 开口和走线方案。

### 阶段 M3：负载装配

1. 安装测试配重或低价值滤镜，检查盘体翘曲和压环受力。
2. 完成所有槽位的光轴中心校验。
3. 对外壳接缝、USB 开口和螺丝孔实施遮光检查。
4. 通过连续换位试验确认橡胶圈无打滑、啃边和明显磨耗。

## 6. 固件实施计划

### 阶段 F1：电机与传感器最小程序

- 实现 4 相半步驱动表、方向控制和非阻塞定时步进。
- 实现速度参数、启动斜坡和停机线圈释放。
- 输出霍尔和微动开关原始状态、消抖状态及触发计数。
- 提供安全停止和移动超时。

### 阶段 F2：回零与槽位标定

- 上电进入 `HOMING`，以低速搜索霍尔原点。
- 首次触发后退出传感区，再以更低速度二次接近，减少回零方向误差。
- 测量完整一圈的总步数和各槽位对应步数，分别保存 5 位、7 位配置。
- 记录微动开关触发点与霍尔原点的关系，确定其作为逐槽索引或独立校验信号的用途。
- 将槽位数、原点偏移、方向、速度、超时及补偿参数保存到 EEPROM，并附带版本号和校验值。

### 阶段 F3：定位算法与状态机

- 实现 `gotoSlot(n)`，支持 5 位和 7 位盘体。
- 计算顺时针和逆时针路径，默认采用较短路径。
- 通过固定方向终到或受控过冲回退处理齿隙、摩擦回差和开关迟滞，最终方案由重复定位数据决定。
- 在运动中报告忙状态，完成后报告实际槽位；传感异常、超时和步数越界进入 `ERROR`。
- 上电、复位、通信重连后执行可配置的回零流程。
- 启用看门狗，并确保 ESP8266 在电机运动期间持续让出网络任务执行时间。

### 阶段 F4：统一控制协议

定义换行结束的 ASCII 协议，首版命令如下：

| 命令 | 作用 |
| --- | --- |
| `ID?` | 返回设备、固件和协议版本 |
| `STATE?` | 返回 `HOMING/READY/MOVING/ERROR` 及错误码 |
| `POS?` | 返回当前槽位；移动中返回 `-1` |
| `HOME` | 启动回零 |
| `GOTO n` | 移动到从 0 开始编号的槽位 |
| `STOP` | 安全停止并进入需确认的状态 |
| `SLOTS 5` / `SLOTS 7` | 设置滤镜盘槽位数 |
| `CAL ...` | 设置或查询标定参数 |
| `SAVE` | 保存有效配置 |

响应格式统一为 `OK ...` 或 `ERR <code> <message>`。Nano 默认串口参数先采用参考项目使用的 57600 baud，完成稳定性测试后冻结协议版本。

### 阶段 F5：ESP8266 网络功能

- 实现 Wi-Fi 初次配置、凭据保存、断线重连和恢复出厂设置。
- 提供本地状态页、手动回零、槽位选择和标定参数页面。
- 将 HTTP 请求转入同一运动状态机，禁止网络回调直接驱动电机。
- 实现 ASCOM Alpaca FilterWheel 所需接口及设备发现。
- 保留串口日志，并提供网络故障时的本地控制入口。

## 7. 上位机与天文软件兼容计划

### 7.1 Arduino Nano 路线

1. 先制作串口测试工具或命令行脚本，覆盖所有命令和错误码。
2. 明确原项目 ASCOM 驱动的串口协议；可兼容时复用协议，不兼容时开发新的 ASCOM FilterWheel 驱动或本地 Alpaca 桥接程序。
3. 在 N.I.N.A. 中验证连接、回零、槽位切换、序列切换和异常恢复。

### 7.2 ESP8266 路线

1. 按 ASCOM Alpaca FilterWheel 接口实现 `Names`、`FocusOffsets` 和 `Position` 等必要成员。
2. 槽位编号使用 0 到 N−1；运动期间 `Position` 返回 `-1`，完成后返回实际槽位。
3. 验证设备发现、并发查询、重复命令、网络中断和主控复位场景。

ASCOM 当前的 COM 与 Alpaca 主接口采用相同的设备语义，FilterWheel 的位置写入应异步启动运动，客户端通过 `Position == -1` 判断移动状态。实现时以官方规范为准：

- https://ascom-standards.org/newdocs/
- https://ascom-standards.org/newdocs/filterwheel.html
- https://ascom-standards.org/AlpacaDeveloper/ASCOMAlpacaAPIReference.html

## 8. 验证与验收标准

### 8.1 台架测试

- 电机连续正反转各 30 分钟，无主控复位、驱动过热和持续丢步。
- 霍尔传感器与微动开关各触发 500 次，无漏触发和重复计数。
- 任意角度启动回零 50 次，成功率 100%。
- 移动超时、传感器常开、传感器常闭和电机断线均产生明确错误码。

### 8.2 整机定位测试

- 5 位或 7 位全部槽位往返切换至少 200 个完整循环。
- 每个槽位从两个方向到达后，滤镜孔相对光轴中心偏差目标值不超过 0.5 mm。
- 测试过程中不允许出现摩擦轮打滑、滤镜盘刮擦和紧固件松动。
- 相邻槽位切换时间目标值不超过 10 秒；最终指标依据电机减速比和可靠速度实测冻结。
- 上电到完成回零的目标时间不超过 30 秒。
- 停机 30 分钟后电机、驱动器和主控温升处于器件允许范围，并记录实测温度。

### 8.3 通信测试

- USB 插拔、串口占用、错误命令和连续快速查询不会导致固件失去响应。
- ASCOM/N.I.N.A. 连续执行 100 次槽位切换，槽位显示与实际位置一致。
- ESP8266 经过 Wi-Fi 断开、路由器重启和 DHCP 地址变化后能够自动恢复。
- 运动过程中网络请求保持可响应，所有重复请求具有确定行为。

## 9. 分阶段交付与退出条件

| 阶段 | 主要产物 | 退出条件 |
| --- | --- | --- |
| P0 资料与需求冻结 | 选型记录、尺寸表、原 BOM 复核结果 | 确定滤镜盘版本、主控首选、电机型号和传感器逻辑 |
| P1 台架电气样机 | 接线图、电流/电平记录、最小测试程序 | 电机与两个传感器稳定运行 |
| P2 Nano 运动固件 | 回零、定位、协议、EEPROM 配置 | 空载定位测试通过 |
| P3 机械整机 | 完整打印件、安装支架、装配记录 | 负载 200 循环测试通过 |
| P4 USB 软件兼容 | 串口工具、ASCOM 兼容层、联调记录 | N.I.N.A. 自动序列切换通过 |
| P5 ESP8266 版本 | 网络配置、Web 页面、Alpaca 服务 | 网络恢复及 Alpaca 测试通过 |
| P6 定版 | BOM、原理图、固件发布包、制作说明 | 全部验收项目完成并保留许可信息 |

## 10. 主要风险与处理措施

| 风险 | 影响 | 处理措施 |
| --- | --- | --- |
| 原 BOM 尚未完成单元格级复核 | 电机、霍尔器件和保护元件型号存在偏差 | 在 P0 使用可控的表格读取工具复核 BOM，并用实物丝印和电气测量交叉确认 |
| 摩擦轮预紧量不稳定 | 打滑、磨耗、定位误差 | 打印 D14–D17 多个版本，记录压缩量，建立固定电机安装尺寸 |
| 5 线电机减速比存在批次差异 | 每圈步数和速度不一致 | 每台设备执行整圈标定，参数写入 EEPROM |
| 微动开关用途判断错误 | 槽位计数逻辑失效 | 手动旋转一周记录波形，再冻结传感器职责 |
| ESP8266 3.3 V 电平与原 5 V 传感器不兼容 | GPIO 损坏或误触发 | 使用 3.3 V 上拉、电平转换和输入保护，装机前测量所有输入电压 |
| 电机电流引起主控复位 | 丢失位置和网络断线 | 星形供电、驱动端储能电容、去耦、线束分离和电压跌落测试 |
| 打印收缩与盘体翘曲 | 摩擦增大、光轴偏移 | 先打印测试件，必要时提高墙数、调整材料和打印方向 |
| 网络控制功能占用实时执行时间 | 步进不均匀或看门狗复位 | 使用定时调度、非阻塞状态机和短网络回调 |
| 参考资料为非商业许可 | 再发布或用途受限 | 保留许可和版权声明，商业计划启动前取得书面授权 |

## 11. 实施前需要冻结的选择

以下默认值可直接进入 P0：

- 首个机械版本：7 × 36 mm 滤镜盘。
- 首个主控：Arduino Nano。
- 首个连接方式：USB 串口，57600 baud。
- 电机基线：5 V、5 线减速步进电机，型号在 P0 复核。
- 传感方案：霍尔原点加微动开关独立校验。
- 网络版本：完成 Nano 整机验收后移植至 ESP8266。

如果实际目标为 5 × 2 英寸滤镜、仅需 Wi-Fi 控制、需要兼容现有原版 ASCOM 驱动，或已经购买了特定电机/开发板，应在 P0 开始时更新上述选择。

